import React from "react";
import ReactDOM from "react-dom/client";

import Provider from "./provider";

import "./index.scss";

ReactDOM.createRoot(document.getElementById("main")).render(<Provider />);
